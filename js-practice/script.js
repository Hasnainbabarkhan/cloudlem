function vowels(str){
    let count = 0;
    for (char of str){ 
        if(
            char === "a" || 
        char === "e" || 
        char === "i" || 
        char === "o" || 
        char === "u"
        ) {
            count++
        }
    }
    return count;
}


// function arrow

const vowels = (str) => {
    let count = 0;
    for (char of str){ 
        if(
        char === "a" || 
        char === "e" || 
        char === "i" || 
        char === "o" || 
        char === "u"
        ) {
            count++
        }
    }
    return count;
}




// practice function

function countVowels(str){
    let count = o;
    for (char of str){
       if(
        char === "a" ||
        char === "e" ||
        char === "i" ||
        char === "o" ||
        char === "u"
       ){
        count++
       }
    }
    return count;
}

// practice function arrow

const countVowels = (str) => {
    let count = 0;
    for (alpha of str){
        if(
            alpha === "a" ||
            alpha === "e" ||
            alpha === "i" ||
            alpha === "o" ||
            alpha === "u" 
        ){
            count++;
        }
    }
    return count;
}


function countThousand(num){
    let number = 0;
    for (number of num){
        if(
            number === 1000
        ){
            count++;
        }
    }
    return count;
}